import { Text, View } from 'react-native';

function SavedPageScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text> Saved Page Screen</Text>
    </View>
   );
 }
 
 export default SavedPageScreen
