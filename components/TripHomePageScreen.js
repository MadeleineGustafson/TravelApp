import { Text, View } from 'react-native';

function TripHomePageScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text> Trip Home Page Screen</Text>
    </View>
   );
 }
 
 export default TripHomePageScreen
